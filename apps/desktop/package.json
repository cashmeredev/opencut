{
	"name": "@opencut/desktop",
	"version": "0.2.0",
	"private": true,
	"main": "dist/main.js",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"dev": "bun run build && electron .",
		"pack": "bun run build && electron-builder --dir",
		"dist": "bun run build && electron-builder --linux AppImage deb rpm",
		"copy:web": "node scripts/copy-web.mjs"
	},
	"dependencies": {
		"ffmpeg-static": "^5.3.0"
	},
	"devDependencies": {
		"@types/node": "^26.1.2",
		"electron": "43.2.0",
		"electron-builder": "^26.15.3",
		"typescript": "^7.0.2"
	},
	"author": "cashmere",
	"homepage": "https://github.com/cashmeredev/opencut",
	"desktopName": "OpenCut"
}
