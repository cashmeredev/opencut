[package]
name = "animation"
version = "0.1.0"
edition = "2024"

[dependencies]
scene = { version = "0.1.0", path = "../scene" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
time = { version = "0.1.0", path = "../time" }
